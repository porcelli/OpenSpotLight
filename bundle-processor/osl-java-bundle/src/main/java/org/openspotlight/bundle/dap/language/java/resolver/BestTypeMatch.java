package org.openspotlight.bundle.dap.language.java.resolver;

/**
 * The Enum that defines the best type match.
 * 
 * @author porcelli
 */
public enum BestTypeMatch {

    /** The First Type is the best match. */
    T1,

    /** The Second Type is the best match. */
    T2,

    /** They are same. */
    SAME
}
